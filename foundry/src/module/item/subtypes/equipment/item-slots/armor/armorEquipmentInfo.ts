import { ArmorItemSlotTemplate } from './types';

export const defaultEquipmentInfo: ArmorItemSlotTemplate = {
	armorBonus: 0,
	type: 'light',
	category: 'cloth',
	checkPenalty: 0,
	speedPenalty: 0,
};
